//! omc-hud — Rust statusline binary for Claude Code.
//!
//! Replaces the TypeScript HUD from oh-my-claudecode with a sub-5ms cold-start
//! single binary. Reads stdin JSON from Claude Code, renders a statusline string
//! to stdout, exits.
//!
//! See workspace README.md and ARCHITECTURE.md (forthcoming) for design.

fn main() {
    eprintln!("omc-hud (skeleton — not yet implemented)");
    std::process::exit(0);
}
